<?xml version="1.0" encoding="UTF-8"?>
<!--
  Structured JSON logging (constitution VI.1): every log line — errors above all — carries
  timestamp, level, logger, message, and whatever's in MDC at the time (session_id/tenant_id,
  set per-request by WebChatController/EmailController) so an operator can trace an incident
  back to the exact customer session and tenant without grepping free-text messages.
-->
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <includeMdcKeyName>session_id</includeMdcKeyName>
            <includeMdcKeyName>tenant_id</includeMdcKeyName>
            <fieldNames>
                <timestamp>timestamp</timestamp>
                <message>message</message>
                <logger>logger</logger>
                <thread>[ignore]</thread>
                <version>[ignore]</version>
                <levelValue>[ignore]</levelValue>
            </fieldNames>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
